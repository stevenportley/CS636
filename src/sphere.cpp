#include "sphere.h"

Sphere::Sphere(Vector3 origin, float radius)
{
    this->origin = origin;
    this->radius = radius;
}

Vector3 Sphere::ray_intersect(const Ray& ray)
{
    

    return {0.0f, 0.0f, 0.0f};

}

